{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Simple Project Sidebar",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}"
      ]
    }
  ]
}
